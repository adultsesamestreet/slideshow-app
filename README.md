# slideshow-app
Creates a series of slides based on images located in a container folder. Uses Node.js.

Create a folder called "images" and then name your folders and HTML files in a similar manner. Update the ```server.js``` file accordingly with new paths and files names.
